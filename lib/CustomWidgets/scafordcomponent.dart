import 'package:flutter/material.dart';

class ScafordComponent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Scaffold Component'),
      ),
    );
  }
}